{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "required": ["lists"],
  "properties": {
    "lists": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "title", "urls", "group", "defaultEnabled"],
        "properties": {
          "id": { "type": "string", "pattern": "^[a-z0-9-]+$" },
          "title": { "type": "string" },
          "urls": { "type": "array", "items": { "type": "string", "format": "uri" }, "minItems": 1 },
          "mirrors": {
            "description": "Fallback source sets tried in order when `urls` fails. Each entry is a complete alternative to `urls` and only counts when every URL in it succeeds.",
            "type": "array",
            "items": {
              "type": "array",
              "items": { "type": "string", "format": "uri" },
              "minItems": 1
            }
          },
          "group": { "enum": ["ads", "privacy", "malware", "annoyances", "regional", "custom", "test"] },
          "lang": { "type": "array", "items": { "type": "string" } },
          "defaultEnabled": { "type": "boolean" },
          "trusted": { "type": "boolean" },
          "homepage": { "type": "string" },
          "license": { "type": "string" },
          "format": { "enum": ["abp", "hosts"] },
          "expires": { "type": "string" }
        }
      }
    }
  }
}
