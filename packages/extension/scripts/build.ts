// Extension build (workstream T4 owns background wiring; T7 owns this script's ruleset/manifest steps).
// See docs/BUILD-AND-RELEASE.md "Extension build".
console.error('extension build: not implemented yet');
process.exit(1);
