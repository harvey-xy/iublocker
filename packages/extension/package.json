{
  "name": "@iublocker/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx scripts/build.ts",
    "build:watch": "tsx scripts/build.ts --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@iublocker/compiler": "workspace:*",
    "@iublocker/scriptlets": "workspace:*",
    "@iublocker/shared": "workspace:*",
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.290",
    "esbuild": "^0.24.0"
  }
}
