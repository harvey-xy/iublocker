#!/usr/bin/env tsx
// CLI entry: iub-compile --lists tools/filterlists.json --cache .cache/lists --out packages/extension/rulesets
// Implemented by workstream T1. Must write dnr/*.json, cosmetic/*.json, scriptlets/*.json,
// scriptlet-groups/*.js, manifest.json, report.json (docs/RULESETS.md §2).
console.error('iub-compile: not implemented yet');
process.exit(1);
